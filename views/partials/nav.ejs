<nav class="main-nav<%= typeof transparentNav !== 'undefined' && transparentNav ? ' main-nav--transparent' : '' %>" aria-label="Main navigation">
  <div class="container nav-container">
    <ul class="nav-list">
      <% navigation.forEach(function(item) { %>
        <% if (item.type === 'link') { %>
          <li class="nav-item">
            <a href="<%= item.path %>" class="nav-link<%= currentPath === item.path ? ' active' : '' %>"><%= item.label %></a>
          </li>
        <% } else if (item.type === 'dropdown') { %>
          <li class="nav-item nav-item--dropdown">
            <a href="#" class="nav-link nav-dropdown-toggle" aria-haspopup="true" aria-expanded="false">
              <%= item.label %> <span class="caret">▾</span>
            </a>
            <ul class="nav-dropdown">
              <% item.children.forEach(function(child) { %>
                <li><a href="<%= child.path %>" class="nav-dropdown-link<%= currentPath === child.path ? ' active' : '' %>"><%= child.label %></a></li>
              <% }); %>
            </ul>
          </li>
        <% } %>
      <% }); %>
    </ul>
  </div>
</nav>
